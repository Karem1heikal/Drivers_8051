


#ifndef LCD_PRIVATE_H_
#define LCD_PRIVATE_H_

static void msdelay(u16 time);  // Function for creating delay in milliseconds.

static u8 Num_length(u16 num);
static u16 Multi_N_10(u8 N);

#endif
